[
  {
    // A match nothing rule to register the category, in case expandedfoods is not installed.
    deletes: [ "meal-breadcrumbs-extra" ]
  },
  {
    dependsOn: [ "expandedfoods" ],
    cooking : {
      recipe: "meatystew",
      ingredient: "breadcrumbs-extra",
      outputs: ["meal-breadcrumbs-extra"]
    }
  }
]
